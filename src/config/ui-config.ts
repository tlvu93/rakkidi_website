export const layoutDimension = {
  drawerWidth: 225,
  headerHeight: 64
};

/**
 * DOM id of the navigation drawer. Shared so the menu buttons can point
 * `aria-controls` at the element they actually toggle.
 */
export const SIDEBAR_ID = 'app-sidebar';
